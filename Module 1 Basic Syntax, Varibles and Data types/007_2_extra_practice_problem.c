#include <stdio.h>

int main() {
/*
*Write all the rules for naming a variable in C programming.
    -Variable name must be start with a letter or underscore.
    -Variable name must contain letter, digits or underscore.
    -Keywords can't be used as variables.
*/
    return 0;
}