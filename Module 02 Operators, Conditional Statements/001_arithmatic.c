#include <stdio.h>

int main() {
   int a =20;
   int b=10;
   int sum=a+b;
   int sub = a-b;
   int mul = a*b;
   int div = a/b;
   printf("Sum of %d and %d is %d\n",a,b,sum);
   printf("Substraction of %d and %d is %d\n",a,b,sub);
   printf("Substraction of %d and %d is %d\n",a,b,sub);
   printf("Substraction of %d and %d is %d\n",a,b,mul);
   printf("Substraction of %d and %d is %d",a,b,div);
    return 0;
}

 /*
*Operator is a symbol which sits between two operand to make an operation.
Arithmetic Operator (+,-,*,/, %)
+ Addition
- Substraction
* Multiplication
/ Division
% Mod(Remainder) (Notice the result whether it is integer or float)
*/