#include <stdio.h>
#include <stdbool.h>

int main() {
    // storing true or false, we will use bool data types.
    bool t= true; // it wil show 1
    bool f= false; // it wil show 0
    printf("%d\n", t); 
    printf("%d", f); 
    return 0;
}